document.addEventListener('DOMContentLoaded',()=>{const cards=[...document.querySelectorAll('.feature-card')];document.querySelectorAll('.submit').forEach(b=>b.onclick=()=>{b.textContent='Submitted ✓';b.disabled=true;b.closest('.feature-card').querySelector('.tag').textContent='Submitted';toast('Assignment submitted successfully')});document.querySelectorAll('.tabs button').forEach(tab=>tab.onclick=()=>{document.querySelector('.tabs .active').classList.remove('active');tab.classList.add('active');const filter=tab.textContent.toLowerCase();cards.forEach(card=>{const status=card.querySelector('.tag').textContent.toLowerCase();card.hidden=!(filter.startsWith('all')||status.includes(filter.includes('due')?'due':filter.includes('submitted')?'submitted':'upcoming'))});toast(`${tab.textContent} assignments shown`)})});
